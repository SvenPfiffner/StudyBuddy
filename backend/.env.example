# ============================================================================
# StudyBuddy Backend Configuration
# ============================================================================
# This file contains environment variables for the StudyBuddy application.
# All variables are prefixed with 'STUDYBUDDY_' as defined in config.py
# Copy this file and customize the values according to your needs.
# ============================================================================

# ----------------------------------------------------------------------------
# External Text API Settings
# ----------------------------------------------------------------------------
# Configure whether to use an external API service for text generation
# (e.g., OpenAI, Anthropic) instead of running local models.

# Set to 'true' to use an external text generation API, 'false' for local models
STUDYBUDDY_USE_EXTERNAL_TEXT_API=false

# Identifier for your external text generation API
# Currently supported: - 
STUDYBUDDY_EXTERNAL_TEXT_API_ID=none

# Base URL for your external text generation API
# Example: https://api.openai.com/v1
STUDYBUDDY_EXTERNAL_TEXT_API_BASE_URL=none

# API key for authenticating with the external text generation service
# Keep this secret and never commit it to version control!
STUDYBUDDY_EXTERNAL_TEXT_API_KEY=none

# ----------------------------------------------------------------------------
# External Image API Settings
# ----------------------------------------------------------------------------
# Configure whether to use an external API service for image generation
# (e.g., DALL-E, Midjourney API) instead of running local models.

# Set to 'true' to use an external image generation API, 'false' for local models
STUDYBUDDY_USE_EXTERNAL_IMAGE_API=false

# Identifier for your external image generation API
# Currently supported: -
STUDYBUDDY_EXTERNAL_IMAGE_API_ID=none

# API key for authenticating with the external image generation service
# Keep this secret and never commit it to version control!
STUDYBUDDY_EXTERNAL_IMAGE_API_KEY=none

# ----------------------------------------------------------------------------
# Local Model Settings
# ----------------------------------------------------------------------------
# Configure which Hugging Face models to use when running locally
# (only applies when external APIs are disabled)

# Hugging Face model ID for text generation
# Default: meta-llama/Llama-3.1-8B-Instruct
# Other options: 'mistralai/Mistral-7B-Instruct-v0.2', 'google/flan-t5-xxl', etc.
STUDYBUDDY_TEXT_MODEL_ID=meta-llama/Llama-3.1-8B-Instruct

# Hugging Face/Diffusers checkpoint ID for image generation
# Default: stabilityai/sdxl-turbo
# Other options: 'runwayml/stable-diffusion-v1-5', 'stabilityai/stable-diffusion-xl-base-1.0', etc.
STUDYBUDDY_IMAGE_MODEL_ID=stabilityai/sdxl-turbo

# ----------------------------------------------------------------------------
# Generation Settings
# ----------------------------------------------------------------------------
# Fine-tune the behavior of text and image generation

# Maximum number of tokens to generate in a single text generation request
# Higher values allow longer responses but consume more resources
# Range: typically 128-4096, default: 1024
STUDYBUDDY_MAX_NEW_TOKENS=1024

# Sampling temperature for the language model
# Lower values (e.g., 0.1) make output more deterministic and focused
# Higher values (e.g., 1.5) make output more creative and diverse
# Range: 0.0-2.0, default: 0.7
STUDYBUDDY_TEMPERATURE=0.7

# Enable or disable image generation feature
# Set to 'true' to generate images alongside text summaries
# Set to 'false' to only generate text (useful for testing or resource constraints)
STUDYBUDDY_ENABLE_IMAGE_GENERATION=false

# ============================================================================
# Notes:
# - Boolean values should be lowercase: 'true' or 'false'
# - Numeric values should not have quotes
# - String values with spaces should be in quotes
# - Never commit this file with real API keys to version control
# - Create a '.env.example' file with placeholder values for sharing
# ============================================================================